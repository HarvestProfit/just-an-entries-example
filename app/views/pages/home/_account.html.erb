<a class="list-group-item list-group-item-action d-flex justify-content-between bg-dark text-light" href="<%= account_path(account) %>%">
  <span class="d-flex align-items-center">
  <%= account.name %>
  <span class="badge bg-primary rounded-pill ml-1">
    <%= account.entries.size %>
  </span>
  </span>
  <%= number_to_currency(account.balance) %>
</a>