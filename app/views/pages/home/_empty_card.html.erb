<div class="card bg-dark" style="min-height: 30vh">
  <div class="card-header">
    No <%= type || 'Things' %>
  </div>
  <div class="card-body d-flex flex-column justify-content-center align-items-center">
    <span class="mb-3">
      There are no <%= type || 'Things' %> yet.
    </span>
    <a href="<%= path || root_url %>" class="btn btn-primary">Add One</a>
  </div>
</div>